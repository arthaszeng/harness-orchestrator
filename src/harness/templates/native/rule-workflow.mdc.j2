---
description: "Harness workflow conventions: commit format, task state management"
alwaysApply: true
---

# Harness Workflow Conventions

## Task State

- All task state lives in `.agents/tasks/task-NNN/`
- Plan: `plan.md`, Build logs: `build-rN.md`, Evaluations: `plan-eval-rN.md` + `code-eval-rN.md`
- Never delete task artifacts — they form the audit trail

## Entry Lock (Workflow Command First)

- If the user explicitly invokes `/harness-*`, you MUST enter that workflow first.
- Before passing that workflow's gate, do NOT modify `src/` or `tests/`.
- First response must echo `Current stage = ...` (Preflight / Plan / ReviewGate / Build / Eval / Ship).
- If current stage is not Build, implementation edits are forbidden.
- Run workspace cleanliness checks required by that workflow before exploration.

## Commit Format

Use conventional commits: `<type>(scope): description`
- feat: new feature
- fix: bug fix
- refactor: code restructuring
- test: adding or updating tests
- docs: documentation changes
- chore: maintenance tasks

## Branch Convention

- Feature branches: `{{ branch_prefix }}/<task-id>-<short-desc>`
- Trunk branch: `{{ trunk_branch }}`
- Never commit directly to trunk
