## CI Verification

Run the full test suite:

```bash
{{ ci_command }}
```

**If tests fail**, apply the Test Failure Ownership Triage:
1. Check if the test also fails on `{{ trunk_branch }}` (pre-existing)
2. Pre-existing failures: note and continue
3. In-branch failures: must fix before proceeding
