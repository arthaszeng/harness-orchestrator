---
name: harness-eval
description: "Multi-role code review with 5 specialized reviewers and Fix-First auto-remediation. Dispatches architect, product-owner, engineer, qa, and project-manager subagents in parallel. Use when saying review this, evaluate, harness eval, or after a build. Proactively invoke after any /harness-build completes. (harness)"
---

{% include 'sections/_language-directive.md.j2' %}
{% include 'sections/_git-strategy-matrix.md.j2' %}

# Harness Eval

You are running the `/harness-eval` workflow. This is a **minimal-interaction** review
pipeline. Run straight through and output the evaluation result at the end.

**Only stop for:**
- ASK-classified findings that need user judgment (see Fix-First below)
- All review subagents failed (no results at all)

**Never stop for:**
- AUTO-FIX findings (fix them, commit, continue)
- Subagent failures (degrade gracefully per degradation ladder)
- Minor issues or informational findings

---

{% include 'sections/_workflow-progress-envelope.md.j2' %}

## Step 0: Context Gathering

1. Detect the current task: find the most recent `task-NNN` directory under `.harness-flow/tasks/`
2. **Check stage summary:** If `TASK_DIR/workflow-state.json` has a non-empty `handoff_summary` field,
   read it first — it provides a compact summary of the build (what was built, CI result,
   decisions, risks, open items). Use this as your primary context for understanding what changed.
   (Backward compat: if `handoff-build.json` exists but `handoff_summary` is empty, read that instead.)
3. **Structured handoffs:** Read `TASK_DIR/handoff-plan.json` and `TASK_DIR/handoff-build.json` when present
   (schema v2). Merge `context_footprint` and narrative fields **before** inferring scope from scratch —
   they list paths and decisions already vetted upstream.
4. Read `.harness-flow/tasks/task-NNN/plan.md` (the contract)
5. Read `.harness-flow/tasks/task-NNN/build-rN.md` (the latest build log)
6. Run `git diff {{ trunk_branch }}..HEAD --stat` to understand the scope
{% if memverse_enabled == "true" %}
7. **Prior Learnings**: Search Memverse for learnings relevant to the changed files/modules.
   If Memverse MCP is available, call `search_memory` with key topics from the diff.
   Use multi-domain OR query (single call replaces dual windows).
   Include the project domain plus all five role-learning domains:
   ```
   search_memory(query=<key topics from diff>, domains="{{ project_name or memverse_domain }},{{ project_name or memverse_domain }}-architect-learning,{{ project_name or memverse_domain }}-engineer-learning,{{ project_name or memverse_domain }}-qa-learning,{{ project_name or memverse_domain }}-product-learning,{{ project_name or memverse_domain }}-project-manager-learning", limit=20)
   ```
   Display any relevant prior learnings (especially pitfalls) before starting the review.
{% endif %}
{% if memverse_enabled == "true" %}8.{% else %}7.{% endif %} If `.harness-flow/tasks/task-NNN/workflow-state.json` exists, treat it as the canonical
   task state file. Evaluation may append or update gate/blocker snapshots there,
   but registry/events must remain audit-only and must not become workflow gate sources.

### Context Degradation Ladder

Check each context source in order. Skip what is missing — do NOT stop for missing context
unless git diff is empty.

| Context Source | If Missing | Impact |
|----------------|------------|--------|
| `task-NNN/` directory | Use branch name as audit ID; write eval artifacts to `.harness-flow/evals/` fallback | No task-level tracking |
| `plan.md` | Warn: "No plan — evaluating raw diff" | Skip contract completeness; score Completeness as N/A |
| `build-rN.md` | Warn: "No build log — skipping builder-claims verification" | Rely on git diff + CI only |
| `handoff-plan.json` / `handoff-build.json` | Skip silently | Less prior-context reuse; do not fail |
| `git diff` (empty) | **FATAL** — nothing to review. Stop and report error. | Cannot proceed |
{% if memverse_enabled == "true" %}
| Memverse MCP | Skip silently | No prior-learnings lookup |
{% endif %}

**Minimum viable eval = non-empty git diff.** Everything else degrades gracefully.

---

{% include 'sections/_trust-boundary.md.j2' %}

---

## Step 1: 5-Role Code Review

{% include 'sections/_code-review.md.j2' %}

---

## Step 2: Capture Learnings

After the evaluation is complete, capture non-obvious insights as learnings:

1. Review all findings with confidence >= 7
2. Parse merged findings for `[LEARNING]` items from each role's output.
   Keep only entries that are:
   - long-lived
   - cross-project / cross-language reusable
   - strongly role-relevant
{% if memverse_enabled == "true" %}
3. **Domain registration guard** (once per session, before the first write):
   Call `manage_domain` for each role domain that will receive writes.
   `manage_domain` is idempotent — registering an already-existing domain is a no-op,
   so **skip `list_domains`** and just register directly:
   ```
   manage_domain(name="{{ project_name or memverse_domain }}-architect-learning", display="Architect Learning", aliases="{{ project_name or memverse_domain }} architect", keywords="architecture learning patterns")
   manage_domain(name="{{ project_name or memverse_domain }}-engineer-learning", display="Engineer Learning", aliases="{{ project_name or memverse_domain }} engineer", keywords="engineering learning patterns")
   manage_domain(name="{{ project_name or memverse_domain }}-qa-learning", display="QA Learning", aliases="{{ project_name or memverse_domain }} qa", keywords="testing qa learning patterns")
   manage_domain(name="{{ project_name or memverse_domain }}-product-learning", display="Product Learning", aliases="{{ project_name or memverse_domain }} product", keywords="product learning patterns")
   manage_domain(name="{{ project_name or memverse_domain }}-project-manager-learning", display="PM Learning", aliases="{{ project_name or memverse_domain }} pm", keywords="project management learning patterns")
   ```
   Fire all 5 calls **in parallel**. If this session already registered domains
   (e.g. eval ran before ship), skip entirely.
4. **Dedup guard** (prevents double-write when ship runs after eval in the same session):
   For each qualifying `[LEARNING]` item, before writing, call:
   ```
   search_memory(query=<learning key or short text>, domains="{{ project_name or memverse_domain }}-<role>-learning", limit=5)
   ```
   If a result with substantially the same insight already exists → **skip** (log as `skipped-dedup`).
   Only write if no near-duplicate is found.
5. For each non-duplicate `[LEARNING]` item, the **parent orchestrator** (you) calls `add_memories`
   with structured metadata (JSON string). Sub-agents do NOT call `add_memories` themselves.
   **Text must include a `[domain]` prefix** to reinforce Memverse domain inference:
   ```
   add_memories(
     text="[{{ project_name or memverse_domain }}-<role>-learning] <learning insight text>",
     domain="{{ project_name or memverse_domain }}-<role>-learning",
     metadata="{\"type\": \"<pattern|pitfall|preference|architecture|tool>\", \"key\": \"<kebab-case-id>\", \"confidence\": <1-10>, \"source\": \"observed\", \"role\": \"<role>\", \"task\": \"<task-id>\", \"files\": [\"<file1>\", \"<file2>\"]}"
   )
   ```
   **Note**: metadata is write-only — `search_memory` cannot filter by metadata fields.
   For precise metadata filtering, use `list_memories` + client-side parsing.
   Treat writes as non-blocking fire-and-forget: do not block gate on write outcome.
   **Parallel writes**: when multiple learnings are ready, dispatch all `add_memories` calls
   concurrently (do not serialize).
6. Log counters: `LEARNING_ADD scheduled|skipped-dedup|skipped|failed-open|domain-registered`.

If Memverse MCP is not available: skip silently.
{% endif %}

---

{% include 'sections/_hook-points.md.j2' %}
