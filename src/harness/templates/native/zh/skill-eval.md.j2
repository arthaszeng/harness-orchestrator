---
name: harness-eval
description: "多角色代码评审，配备 5 个专业审查者和 Fix-First 自动修复。并行派遣 architect、product-owner、engineer、qa 和 project-manager 子代理。当用户说 review this、evaluate、harness eval 或在 build 之后使用。在任何 /harness-build 完成后主动调用。(harness)"
---

{% include 'sections/_language-directive.md.j2' %}

# Harness Eval

你正在运行 `/harness-eval` 工作流。这是一个**最少交互**的评审管线。
直接运行到底并在最后输出评审结果。

**仅在以下情况停止：**
- 需要用户判断的 ASK 类发现（见下方 Fix-First）
- 所有评审子代理都失败（完全没有结果）

**绝不因以下原因停止：**
- AUTO-FIX 发现（修复、提交、继续）
- 子代理失败（按降级阶梯优雅降级）
- 次要问题或信息性发现

---

## 步骤 0：上下文收集

1. 检测当前任务：在 `.agents/tasks/` 下找到最新的 `task-NNN` 目录
2. **检查交接文件：** 如果 `TASK_DIR/handoff-build.json` 存在，优先读取 —
   它提供构建结果的紧凑摘要（构建了什么、CI 结果、决策、风险、开放项）。
   以此为主要上下文来理解发生了什么变更。
3. 读取 `.agents/tasks/task-NNN/plan.md`（合约）
4. 读取 `.agents/tasks/task-NNN/build-rN.md`（最新构建日志）
5. 运行 `git diff {{ trunk_branch }}..HEAD --stat` 了解范围
{% if memverse_enabled == "true" %}
6. **先验学习**：搜索 Memverse 获取与变更文件/模块相关的学习成果。
   如果 Memverse MCP 可用，使用 diff 中的关键主题调用 `search_memory`。
   使用双窗口：
   - project recent：domain `{{ project_name or memverse_domain }}`，limit 20
   - role learning：domain `{{ project_name or memverse_domain }}-<role>-learning`，limit 15
   在开始评审前展示任何相关的先验学习成果（尤其是陷阱）。
{% endif %}
{% if memverse_enabled == "true" %}7.{% else %}6.{% endif %} 如果 `.agents/tasks/task-NNN/workflow-state.json` 存在，把它视为 canonical task state。
   评估可以在其中追加或更新 gate / blocker snapshot，但 registry / events 仍只用于审计，
   不得成为 workflow gate 来源。

### 上下文降级阶梯

按顺序检查每个上下文来源。跳过缺失的内容 — 除非 git diff 为空，否则不要停止。

| 上下文来源 | 如果缺失 | 影响 |
|------------|----------|------|
| `task-NNN/` 目录 | 使用分支名作为审计 ID；将评审产物写入 `.agents/evals/` 备用路径 | 无任务级跟踪 |
| `plan.md` | WARN: "无计划 — 评审原始 diff" | 跳过合约完整性；Completeness 记为 N/A |
| `build-rN.md` | WARN: "无构建日志 — 跳过对 builder 声明的核验" | 仅依赖 git diff + CI |
| `git diff`（为空） | **FATAL** — 无可评审内容。停止并报错。 | 无法继续 |
{% if memverse_enabled == "true" %}
| Memverse MCP | 静默跳过 | 无先验学习查询 |
{% endif %}

**最低可行评审 = 非空 git diff。** 其他一切优雅降级。

---

{% include 'sections/_trust-boundary.md.j2' %}

---

## 步骤 1：5 角色代码评审

{% include 'sections/_code-review.md.j2' %}

---

## 步骤 2：捕获学习成果

评审完成后，将非显而易见的见解捕获为学习成果：

1. 审查所有置信度 >= 7 的发现
2. 解析 merged findings 中的 `[LEARNING]` 条目，仅保留同时满足：
   - 可长期有效
   - 可跨项目 / 跨语言复用
   - 与对应角色高度相关
{% if memverse_enabled == "true" %}
3. 对每个角色的 `[LEARNING]` 条目，best-effort 调度 `add_memories`
   写入 `{{ project_name or memverse_domain }}-<role>-learning`。
   采用 non-blocking fire-and-forget，不以写入结果阻塞 gate。
4. 记录计数：`LEARNING_ADD scheduled|skipped|failed-open`。

如果 Memverse MCP 不可用：静默跳过。
{% endif %}

---

{% include 'sections/_hook-points.md.j2' %}
