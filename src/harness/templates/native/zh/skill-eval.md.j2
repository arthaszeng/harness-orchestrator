---
name: harness-eval
description: "多角色代码评审，配备 5 个专业审查者和 Fix-First 自动修复。并行派遣 architect、product-owner、engineer、qa 和 project-manager 子代理。当用户说 review this、evaluate、harness eval 或在 build 之后使用。在任何 /harness-build 完成后主动调用。(harness)"
---

{% include 'sections/_language-directive.md.j2' %}
{% include 'sections/_git-strategy-matrix.md.j2' %}

# Harness Eval

你正在运行 `/harness-eval`。**最少交互**：一路执行到底并输出评审结果。

**仅在以下情况停止：** 需用户判断的 ASK 发现（Fix-First）；所有评审子代理均失败且无结果。

**不要因以下情况停止：** AUTO-FIX（修复、提交、继续）；部分子代理失败（降级）；次要/仅 INFO 噪声。

---

{% include 'sections/_workflow-progress-envelope.md.j2' %}

**会话边界：** 每完成一个编号步骤后，用 `harness task resolve --json` 取 `TASK_ID`，持久化 `SessionContext`（`current_phase` 为 `"eval"`）：

```bash
echo '{"task_id":"TASK_ID","current_phase":"eval","current_step":"N","current_state":"<简要>","next_step":"<下一步>"}' | harness session write --task TASK_ID
```

## 步骤 0：上下文收集

1. `harness task resolve --json` → `TASK_DIR` / `task_id`。
2. 若有 `has_handoff_summary` 或 `workflow-state.json` → 读 `handoff_summary` / `handoff-build.json`（兼容）。
3. 若存在则读 `handoff-plan.json` / `handoff-build.json`（v2 `context_footprint`）。
4. `plan.md`、最新 `build-rN.md`、`git diff {{ trunk_branch }}..HEAD --stat`。
{% if memverse_enabled == "true" %}
5. Memverse：单次 `search_memory` 覆盖项目域与五个 `*-learning` 域（形状同 ship 步骤 1）。
{% endif %}
{% if memverse_enabled == "true" %}6.{% else %}5.{% endif %} `workflow-state.json` 为门禁权威；registry/events 仅审计。

### 上下文降级阶梯（Context Degradation Ladder）

| 来源 | 缺失时 |
|------|--------|
| 任务目录 | 回退 `.harness-flow/evals/` |
| plan / 构建日志 | 警告并继续 |
| git diff 为空 | **FATAL** — 停止 |

**最低可行评审（Minimum viable eval）** = 非空 git diff。完整阶梯与子代理降级见 code-review-protocol.md。

---

{% include 'sections/_trust-boundary.md.j2' %}

---

## 步骤 1：5 角色代码评审

**必须先阅读** `.cursor/skills/harness/harness-eval/code-review-protocol.md` **中的协议再继续此步骤。**

通过 Task 工具派遣 `harness-architect`、`harness-product-owner`、`harness-engineer`、`harness-qa`、`harness-project-manager`。各角色按协议输出 **Value recommendation** / **Delivery recommendation** 与分数。在子代理提示词事实块中包含 `response_language: {{ artifact_lang }}`。


{% include 'sections/_failure-pattern-save.md.j2' %}

{% include 'sections/_error-recovery.md.j2' %}

---

## 步骤 2：捕获学习成果

评审结束后：仅持久化可跨会话的 `[LEARNING]`（置信度 ≥7）。**绝不**写数量、PR#、hash、分数或复述任务描述。

{% if memverse_enabled == "true" %}
五个 `*-learning` 域并行 `manage_domain`（幂等）。去重：`search_memory`。`add_memories`：`[domain]` 文本前缀 + metadata JSON（`type`、`key`、`confidence`、`source`、`role`、`task`、`files`）。精确键匹配优先 `upsert_memory`。并行写入；记 `LEARNING_ADD`。MCP 缺失则静默跳过。
{% else %}
跳过 — Memverse 未启用。
{% endif %}

---

{% include 'sections/_hook-points.md.j2' %}
