---
name: harness-ship
description: "最少交互的交付管线：合并基准、测试、自适应代码评审（基于 escalation score 的 FULL/LITE/FAST）、修复循环、可二分提交、推送、PR。一个命令 — 你看到的下一个东西就是 PR URL。当用户说 ship this、harness ship、push 或 create a PR 时使用。当用户说代码已完成或要求部署时主动调用。(harness)"
---

{% include 'sections/_language-directive.md.j2' %}
{% include 'sections/_git-strategy-matrix.md.j2' %}
{% include 'sections/_continuity-mandate.md.j2' %}

# Harness Ship — 自动化管线

你正在运行 `/harness-ship`。**最少交互**：一路执行到底并输出 PR URL。

**仅在以下情况停止：** 位于 `{{ trunk_branch }}`；rebase 冲突无法解决；分类后仍有分支内测试失败；ASK 类发现；在 {{ max_iterations }} 轮评审后分数仍低于 {{ pass_threshold }}。

**不要因以下情况停止：** 未提交变更；提交消息或 PR 审批；AUTO-FIX 发现。

---

{% include 'sections/_workflow-progress-envelope.md.j2' %}

{% with preflight_mode="ship" %}
{% include 'sections/_workspace-preflight.md.j2' %}
{% endwith %}

**会话边界：** 每完成一个编号步骤后持久化上下文。用 `harness task resolve --json` 取 `TASK_ID`。stdin 传入符合 `SessionContext` 的 JSON（如 `task_id`、`current_phase` 为 `"ship"`、`current_step`、`current_state`、`next_step`）：

```bash
echo '{"task_id":"TASK_ID","current_phase":"ship","current_step":"N","current_state":"<简要>","next_step":"<下一步>"}' | harness session write --task TASK_ID
```

## 步骤 1：预检

**快速路径：** `harness task resolve --json` → `TASK_DIR`。若 `workflow-state.json` 有效、`phase` ∈ {contracted, building, evaluating, shipping}、`has_handoff_summary`、分支匹配 `{{ branch_prefix }}/` → 复用 `TASK_DIR`、读 handoff/plan/state，**仍须** `git fetch origin {{ trunk_branch }}`，再进步骤 2。

**完整：** 不得在 `{{ trunk_branch }}`（否则中止）。`git status`；`git diff {{ trunk_branch }}...HEAD --stat` 与 `git log {{ trunk_branch }}..HEAD --oneline`。存在 `.harness-flow/config.toml`。任务目录：有 `plan.md` 的最大 `task-NNN` 否则新建；读 `handoff_summary` / `handoff-*.json` / `handoff-plan.json` / `handoff-build.json` / `plan.md`；以 `workflow-state.json` 为准。
{% if memverse_enabled == "true" %}
Memverse：对项目域与五个 `*-learning` 域执行 `search_memory`（查询形状同 eval）。
{% endif %}
**评审就绪提醒** — Todo 清单须含 **EVAL — 强制**（步骤 3.8）再提交。

---

## 步骤 2：Rebase（测试前）

```bash
git fetch origin {{ trunk_branch }} && git rebase origin/{{ trunk_branch }}
```

冲突：简单可自解，否则 `git rebase --abort` 并 **停止**。

---

## 步骤 3：运行测试

```bash
{{ ci_command }}
```

失败时：**必须先阅读** `.cursor/skills/harness/harness-ship/ship-test-triage.md` **中的协议再继续此步骤。** 基线也失败 → 记录并继续；仅分支失败 → 修复（步骤 5）。全过 → 继续。

{% include 'sections/_error-recovery.md.j2' %}

{% include 'sections/_failure-pattern-save.md.j2' %}

---

## 步骤 3.4：测试覆盖率审计

分析 diff，确保变更代码路径有足够测试覆盖。

**必须先阅读** `.cursor/skills/harness/harness-ship/ship-coverage-audit.md` **中的协议再继续此步骤。**

---

## 步骤 3.5：计划完成度审计

与步骤 3.4（覆盖率审计）并行运行：

```bash
harness plan-completion-audit --task TASK_ID --json
```

返回 `{deliverables: [{id, title, status, evidence}], summary: {done, partial, not_done, changed}}`。
NOT DONE 项 → 写入 PR 正文；不阻塞。

---

## 步骤 3.8：预着陆评审 — 自适应代码评估（Adaptive Code Evaluation）

{% include 'sections/_ship-review-gate.md.j2' %}

**派遣：** FULL / LITE → 全部 5 角色（`harness-architect`、`harness-product-owner`、`harness-engineer`、`harness-qa`、`harness-project-manager`），LITE 仅合成放宽；FAST → 不派子代理（gate 已写产物）。

**必须先阅读** `.cursor/skills/harness/harness-eval/code-review-protocol.md` **中的协议再继续此步骤。**

{% if review_gate == 'eng' %}

**Review Gate (eng)：** 评审为硬门禁；ITERATE 阻塞；CRITICAL 须解决后再继续。
{% elif review_gate == 'advisory' %}

**Review Gate (advisory)：** ITERATE 仅记录；CRITICAL 标记；advisory 模式下 **does not block**（不因 ITERATE 单独阻塞交付）。
{% endif %}

---

## 步骤 3.9：捕获学习成果

{% if memverse_enabled == "true" %}
合成后：只写可长期复用的 `[LEARNING]`（置信度 ≥7），不写日志类（数量、PR#、hash、分数）。各角色域一次性并行 `manage_domain`；`search_memory` 去重；`add_memories` 带 `[domain]` 前缀与 metadata；并行写入；记 `LEARNING_ADD`。写入失败不阻塞门禁。
{% else %}
跳过 — Memverse 未启用。
{% endif %}

---

## 步骤 4：范围漂移检测

计划 + 提交 + TODOS 对照 `git diff {{ trunk_branch }}...HEAD --stat` → 一行 CLEAN/DRIFT/MISSING；仅供参考。

---

## 步骤 5：修复循环（若 ITERATE）

按反馈改 → `{{ ci_command }}` → 重跑步骤 3.8；最多 {{ max_iterations }} 轮。否则 **停止** 并报告分数/发现。

---

## 步骤 5.5：TODOS.md

若存在：与 diff 对账；已完成移入 `## Completed`；有变更则 `git add TODOS.md && git commit -m "chore: update TODOS.md"`。

---

## 步骤 5.9：Eval 产物门禁（强制）

```bash
harness gate --task TASK_ID
```

0 → 继续。1 → 处理（缺 eval → 步骤 3.8；ITERATE → 步骤 5；stale → 视情况重评）。{% if review_gate == 'advisory' %}Advisory：ITERATE 仅警告，除非 eval 缺失/空。{% else %}Eng：ITERATE 阻塞；仅 PASS 可进步骤 6。分数 ≥ {{ pass_threshold }} 在步骤 3.8 执行。{% endif %}

---

## 步骤 5.95：提取失败模式

若本次任务经历了 ITERATE 轮次、测试失败或 CI 错误且已修复，扫描任务产物（`code-eval-rN.md`、`build-rN.md`），记录有已知根因 + 修复的模式。零失败 → 跳过。

对每个失败：`harness search-failures --query "<关键词>" --limit 3` 去重后：
```bash
harness save-failure --task TASK_ID --phase <阶段> --category <类别> \
  --summary "<描述>" --root-cause "<根因>" --fix "<修复>"
```
目标每次 ship 0-3 个高质量模式。

---

## 步骤 6：提交（可二分）

{% if review_gate == 'advisory' %}评审完成后（advisory）。{% else %}PASS 后。{% endif %} 分组：基础设施 → 逻辑+测试；`feat|fix|refactor|test|docs|chore`；`git add <files> && git commit -m "<type>(scope): msg"`。

---

{% include 'sections/_bypass-immunity.md.j2' %}

---

## 步骤 7：验证门禁

若自上次完整 `{{ ci_command }}` 后有代码文件变更则重跑；否则仅文档/配置且已在步骤 5 验证则可跳过。失败 → 步骤 3。`git status` 须干净。

{% if hooks_pre_build or hooks_post_eval or hooks_pre_ship %}
---

{% include 'sections/_hook-points.md.j2' %}

---
{% endif %}

## 步骤 7.5：推送前 rebase

```bash
git fetch origin {{ trunk_branch }} && git rebase origin/{{ trunk_branch }}
```

冲突策略同步骤 2。

---

## 步骤 8：推送 + PR

```bash
git push -u origin HEAD
```

**必须先阅读** `.cursor/skills/harness/harness-ship/ship-pr-protocol.md` **中的协议再继续此步骤。** 输出 PR URL（无 `gh` 则输出分支与远程）。

---

## 步骤 8.5：Doc-Release

`harness diff-stat --json` → `has_md_changes` 为真则跑 `/harness-doc-release`；有修正则提交。

---

## 步骤 8.75：Ship 指标

`harness save-ship-metrics --task TASK_ID ...`（或追加 `.harness-flow/ship-metrics.jsonl`）。若持久化 workflow state，最后再 `harness gate --task TASK_ID`。

---

## 步骤 9：PR 着陆

无 `gh` → 跳过并提示人工。否则轮询 ≤3 次：`harness pr-status --branch "$(git rev-parse --abbrev-ref HEAD)" --json`；通过且无冲突 → 成功；冲突 → rebase + `git push --force-with-lease`；失败 → `harness ci-logs`、修复、`{{ ci_command }}`、提交、推送；pending → 退避（总上限约 15m）。**停止**条件：纯基础设施、需人工、轮次用尽、冲突无解。成功：更新 `workflow-state.json`，输出 merge-ready 摘要。

---

## Ship 交接

`workflow-state.json` 的 `handoff_summary`（≤2000 字）：PR URL、提交数、分数、发现、着陆状态。

---

## 恢复（简表）

| 产物 / phase | 从哪步继续 |
|--------------|------------|
| 仅 plan，contracted | 步骤 1 快速 |
| + build，building | 步骤 3 |
| eval ITERATE，evaluating | 步骤 5（Step 5） |
| eval PASS，evaluating | 步骤 5.9 |
| gate PASS 未推送，shipping | 步骤 6 |
| 已推送无 PR | 步骤 8 |
| PR CI pending/fail，landing | 步骤 9 轮询 |
| PR 全绿，landing | 步骤 9 结束 |

代码未变则勿重跑已通过 eval/测试。

---

## 重要规则

- **禁止跳过** 评审（3.8）与 **gate**（5.9）。仅测试不够。
- **禁止跳过** ship review gate 强度规则；派遣失败按 **code-review-protocol.md** 降级。
- **禁止 force push**，步骤 9 解决冲突时可用 `--force-with-lease`。
- **不问琐碎确认** — 自动提交/推送/PR。
- **可二分提交**；**推送前须有新鲜验证**。
- **目标：** `/harness-ship` → 下一条输出即 PR URL。

长期迭代 SSOT：`feedback_ledger_path`、`continue_pause_summary` — 见 `/harness-vision`。
