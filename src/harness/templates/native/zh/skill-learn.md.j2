---
name: harness-learn
description: "使用 Memverse MCP 作为存储后端的结构化学习记忆管理。跨会话搜索、添加、清理和导出学习成果。与评审和交付工作流集成以获取先验知识。(harness)"
---

{% include 'sections/_language-directive.md.j2' %}

{% if memverse_enabled == "true" %}
# Harness Learn — 结构化学习记忆

管理跨会话持久化的项目学习成果。使用 Memverse MCP 进行
语义存储和检索。

## 命令

### `/harness-learn` — 显示最近的学习成果
显示此项目最近的 20 个学习成果。

1. 使用查询 "learnings for {{ memverse_domain }}" 调用 Memverse `search_memory`
2. 以表格格式展示：

```
RECENT LEARNINGS ({{ memverse_domain }})
═══════════════════════════════════════════════════
  #  Type         Key                    Confidence
  1  pattern      retry-with-backoff      9/10
  2  pitfall      async-in-sync-context   8/10
  3  preference   pytest-over-unittest    7/10
═══════════════════════════════════════════════════
```

### `/harness-learn search <query>` — 语义搜索
按含义搜索学习成果。

1. 使用用户的查询和 domain "{{ memverse_domain }}" 调用 Memverse `search_memory`
2. 展示匹配的学习成果及相关性评分
3. 显示每个匹配的完整见解文本

### `/harness-learn add` — 添加学习成果
手动记录新的学习成果。

提示输入（或从上下文中提取）：
- **type**：pattern / pitfall / preference / architecture / tool
- **key**：短标识符（kebab-case）
- **insight**：实际的学习内容（1-3 句话）
- **confidence**：1-10（这个学习有多确定？）
- **files**：相关文件（可选）

写入前确保目标域已注册（`manage_domain` 是幂等的）：
```
manage_domain(name="{{ memverse_domain }}", display="{{ memverse_domain }}", aliases="{{ memverse_domain }}", keywords="{{ memverse_domain }} learning")
```

通过 Memverse `add_memories` 以结构化 metadata（JSON 字符串）存储。
**text 必须包含 `[domain]` 前缀**以强化 Memverse 域推断：
```
add_memories(
  text="[{{ memverse_domain }}] {insight}",
  domain="{{ memverse_domain }}",
  metadata="{\"type\": \"{type}\", \"key\": \"{key}\", \"confidence\": {confidence}, \"source\": \"user-stated\", \"files\": [\"{files}\"], \"branch\": \"{branch}\", \"commit\": \"{commit}\"}"
)
```

**注意**：`search_memory` 支持 `metadata_filter` 做召回后过滤（v5.28.0+）：
`search_memory(query=..., metadata_filter="{\"type\": \"pitfall\"}")`。
需要按 metadata key 精确匹配（如按 `key` 去重），优先使用 `upsert_memory`（含 `upsert_key`）。
若 `upsert_memory` 不可用，回退到 `add_memories` + `search_memory` 去重。

### `/harness-learn prune` — 检查陈旧性
审查已有的学习成果是否陈旧。

1. 通过 `list_memories(domain="{{ memverse_domain }}", limit=100)` 获取此项目的所有学习成果
   （使用 `list_memories` 做完整枚举 — `search_memory` 可能遗漏语义相似度低的条目）。
   返回值含 `total`（域内总数）和 `has_more`。若 `has_more` 为 true，使用 `offset` 继续分页。
2. 对引用了具体文件的每个学习成果：
   - 检查文件是否仍然存在
   - 如果已删除：标记为可能已陈旧
3. 检查矛盾：
   - 关于同一主题说相反话的两个学习成果
   - 与当前代码模式矛盾的学习成果
4. 输出报告：

```
LEARNING HEALTH CHECK
═══════════════════════════════════════
  Stale (file deleted):  N learnings
  Contradictions found:  M pairs
  Healthy:               K learnings
═══════════════════════════════════════
```

5. 对陈旧/矛盾的学习成果：
   - 如果矛盾可以通过更新解决：使用 `update_memory(memory_id, new_content, metadata="{...}")` 合并/修正
     （`update_memory` 现支持可选 `metadata` 参数做浅合并更新——新 key 添加、已有 key 覆盖、未提及 key 保留）
   - 如果确实过时：通过 `delete_memories` 建议删除

### `/harness-learn export` — 导出为 Markdown
按类型分组导出所有学习成果。

```markdown
# Project Learnings: {{ memverse_domain }}

## Patterns
- **retry-with-backoff** (9/10): Always use exponential backoff for external API retries...

## Pitfalls
- **async-in-sync-context** (8/10): Never call sync DB operations inside async handlers...

## Preferences
- **pytest-over-unittest** (7/10): Project uses pytest exclusively, never unittest...

## Architecture
- **event-driven-processing** (9/10): All background jobs use the event queue...

## Tool
- **ruff-over-flake8** (8/10): Project uses ruff for linting, not flake8...
```

### `/harness-learn stats` — 统计信息
显示学习统计。

```
LEARNING STATISTICS
═══════════════════════════════════════
  Total:           N
  By type:
    pattern:       X
    pitfall:       Y
    preference:    Z
    architecture:  W
    tool:          V
  Avg confidence:  X.X/10
  By source:
    observed:      A
    user-stated:   B
    inferred:      C
═══════════════════════════════════════
```

---

## 记忆 Schema

每个学习成果通过 `add_memories` 存储，包含两个部分：

**`text` 参数**（可搜索的内容）：
学习见解的纯文本（1-3 句话）。这是 `search_memory` 匹配的内容。

**`metadata` 参数**（JSON 字符串）：
```json
{
  "type": "pattern | pitfall | preference | architecture | tool",
  "key": "short-kebab-case-id",
  "confidence": 9,
  "source": "observed | user-stated | inferred | cross-model",
  "role": "architect | engineer | qa | product-owner | project-manager",
  "task": "task-NNN",
  "files": ["src/api.py"],
  "branch": "agent/task-041-...",
  "commit": "abc1234"
}
```

**Metadata 过滤**（v5.28.0+）：`search_memory` 支持 `metadata_filter` 做召回后过滤：
`search_memory(query=..., metadata_filter="{\"type\": \"pitfall\", \"confidence\": {\"$gte\": 7}}")`。
标量值精确匹配；数值比较支持 `$gte`、`$gt`、`$lte`、`$lt`、`$eq`。
注意：`metadata_filter` 是**召回后过滤**——未进入向量召回候选集的条目可能被遗漏。
完整枚举请使用 `list_memories` + 调用方解析。

**Upsert**（v5.33.0+）：使用 `upsert_memory` 按 metadata key 创建或更新：
```
upsert_memory(content=..., metadata="{\"key\": \"my-learning-key\", ...}", upsert_key="key", domain="{{ memverse_domain }}")
```
无匹配 → 创建；1 个匹配 → 更新（content 替换，metadata 浅合并）；多个匹配 → 返回 `UPSERT_CONFLICT`。
若 `upsert_memory` 不可用，回退到 `add_memories` + `search_memory` 去重。

---

## 与评审/交付工作流的集成

### 先验学习搜索（自动）

在 `/harness-eval` 和 `/harness-ship` 工作流开始时：

1. 从 diff 中提取关键主题（文件名、模块名、使用的模式）
2. 使用多域 OR 查询调用 Memverse `search_memory`（与 eval/ship 对齐）：
   `search_memory(query=<topics>, domains="{{ memverse_domain }},{{ memverse_domain }}-architect-learning,{{ memverse_domain }}-engineer-learning,{{ memverse_domain }}-qa-learning,{{ memverse_domain }}-product-learning,{{ memverse_domain }}-project-manager-learning", limit=20)`
3. 如果找到相关学习成果，展示：

```
PRIOR LEARNINGS (relevant to this diff):
  - [pitfall] async-in-sync-context: Never call sync DB operations inside async handlers.
    Relevant because: diff modifies async endpoint handlers.
```

4. 将先验学习纳入评审 — 已知陷阱应对照 diff 进行检查。

### 捕获学习成果（自动）

在 `/harness-eval` 和 `/harness-ship` 工作流结束时：

1. **域注册守卫**（每个会话一次）：
   对每个将接收写入的角色域调用 `manage_domain`（幂等，跳过 `list_domains`）：
   ```
   manage_domain(name="{{ memverse_domain }}-architect-learning", display="Architect Learning", aliases="{{ memverse_domain }} architect", keywords="architecture learning patterns")
   manage_domain(name="{{ memverse_domain }}-engineer-learning", display="Engineer Learning", aliases="{{ memverse_domain }} engineer", keywords="engineering learning patterns")
   manage_domain(name="{{ memverse_domain }}-qa-learning", display="QA Learning", aliases="{{ memverse_domain }} qa", keywords="testing qa learning patterns")
   manage_domain(name="{{ memverse_domain }}-product-learning", display="Product Learning", aliases="{{ memverse_domain }} product", keywords="product learning patterns")
   manage_domain(name="{{ memverse_domain }}-project-manager-learning", display="PM Learning", aliases="{{ memverse_domain }} pm", keywords="project management learning patterns")
   ```
   5 个调用并行发出。如果本会话已注册过，则跳过。
2. 审查评估中的所有发现
3. 对任何非显而易见的发现（置信度 >= 7）且揭示了模式或陷阱的：
   - **去重守卫**：写入前在目标角色学习域中 `search_memory` 该 learning key；
     若已存在近似重复 → 跳过（记录为 `skipped-dedup`）
   - 由**父协调器**通过 Memverse `add_memories` 自动捕获为学习成果（附带 metadata）
   - **text 必须包含 `[domain]` 前缀**（如 `[{{ memverse_domain }}-qa-learning] 见解文本`）
   - 子代理不得自行调用 `add_memories` — 它们在输出契约中输出 `[LEARNING]` 条目
   - Type：pattern（如果是好的实践）或 pitfall（如果是常见错误）
   - Source："observed"
   - Confidence：与发现的置信度相同
   - **并行写入**：所有 `add_memories` 调用并发 dispatch
4. 记录："从本次评审中捕获了 N 个新的学习成果，M 个 skipped-dedup，K 个 domain-registered"

---

## 重要规则

- **始终使用 Memverse MCP** 作为存储后端 — 不要为学习成果创建本地文件
- **域隔离** — 始终限定在 "{{ memverse_domain }}" 域
- **去重** — 添加之前，搜索具有相似 key 的已有学习成果
- **置信度衰减** — 被新证据矛盾的学习成果应该通过 `update_memory(memory_id, new_content, metadata="{...}")` 更新，
  而不是在旁边添加重复条目（metadata 浅合并：新 key 添加、已有 key 覆盖、未提及 key 保留）
- **Upsert 复现模式** — 同一 learning key 复现时，优先使用
  `upsert_memory(content=..., metadata=..., upsert_key="key", domain=...)` 而非 add+去重
- **隐私** — 绝不将密钥、凭证或 PII 作为学习成果存储

{% else %}
> 本项目未启用 Memverse。如需启用，请在 `.harness-flow/config.toml` 中设置 `integrations.memverse.enabled = true` 并运行 `harness update`。
{% endif %}
