---
name: harness-investigate
description: "系统化调试工作流，包含根因分析、假设测试、范围锁定和 3 次失败升级。遇到 bug、测试失败或意外行为时使用。在提出任何修复之前调用。(harness)"
---

{% include 'sections/_language-directive.md.j2' %}

# Harness Investigate — 系统化调试

你正在运行 `/harness-investigate` 工作流。这是一个**严谨的调试过程**。
不要急着修复。先调查。

```
铁律：没有根因调查，不做任何修复。

你是侦探，不是修理工。
在动任何东西之前，先搞清楚发生了什么。
```

**危险信号 — 如果你发现自己这样想，立刻停下：**
- "先临时修一下" → 你还不理解问题。
- "让我先试试这个修复" → 那是猜测，不是调查。
- "先修再追踪" → 顺序反了。先追踪。
- "每次修复都暴露新问题" → 你在追逐症状，不是根因。

---

## 阶段 1：根因调查

### 1.1 收集症状
1. 收集所有错误消息、堆栈跟踪和日志
2. 记录触发问题的确切步骤序列
3. 记录预期行为应该是什么
4. 记录实际发生了什么
5. 记录上次正常工作是什么时候（如已知）

### 1.2 阅读代码
1. 阅读涉及的具体代码路径（不要浏览 — 逐行阅读）
2. 从输入到失败点追踪数据流
3. 在每个边界交叉处检查类型
4. 记录代码对其输入做出的任何假设

### 1.3 检查最近的变更
1. 运行 `git log --oneline -20` 查看最近的提交
2. 运行 `git diff HEAD~5..HEAD -- <affected-files>` 查看受影响文件的最近变更
3. 确认问题是否由某个特定提交引入
4. 如果是：`git show <commit>` 理解完整变更

### 1.4 复现
1. 写出最小复现步骤
2. 确认你能按需触发 bug
3. 如果无法复现：记录此情况并继续代码分析

### 1.5 范围锁定
一旦确认了受影响的区域，**锁定你的编辑范围**：
- 只修改受影响目录中的文件
- 不要"顺便"修复无关的东西
- 任何范围外的观察都记入单独的笔记，而不是代码变更

---

## 阶段 2：模式分析

将症状与常见的失败模式对照检查：

| 模式 | 症状 | 调查方向 |
|------|------|----------|
| **竞态条件** | 间歇性失败，有时正常有时失败，负载下失败 | 检查共享可变状态、缺失的锁、非原子操作 |
| **空值传播** | 在意外位置出现 TypeError/NoneType/undefined | 向后追踪 None/null 值，找到它应该被设置的地方 |
| **状态损坏** | 特定操作序列后行为错误 | 检查状态机转换，查找部分更新 |
| **集成不匹配** | 隔离测试正常，组合时失败 | 检查服务边界处的数据格式假设 |
| **配置漂移** | 在一个环境正常，在另一个环境失败 | 比较配置、环境变量、依赖版本 |
| **缓存陈旧** | 返回过期数据，或间歇性返回正确数据 | 检查 TTL、失效逻辑、缓存键构造 |
| **资源耗尽** | 逐渐退化，然后突然失败 | 检查连接泄漏、内存泄漏、文件句柄泄漏 |

---

## 阶段 3：假设测试

基于阶段 1-2 的分析形成假设。系统地测试它们。

**3 次规则：** 如果连续 3 个假设错误，**停止**。
- 输出你到目前为止学到的内容
- 描述失败的假设以及它们为什么错了
- 在继续之前向用户请求额外的上下文

对每个假设：
1. 清楚地陈述假设："我认为 bug 是由 X 引起的，因为 Y"
2. 预测："如果这个假设正确，那么 Z 应该是可观察的"
3. 测试：运行一个能确认或反驳的具体检查
4. 记录结果：CONFIRMED / REFUTED / INCONCLUSIVE

```
HYPOTHESIS LOG:
═══════════════════════════════════════
  #1: [假设] → [CONFIRMED/REFUTED/INCONCLUSIVE]
      证据: [你观察到了什么]
  #2: [假设] → [CONFIRMED/REFUTED/INCONCLUSIVE]
      证据: [你观察到了什么]
═══════════════════════════════════════
```

---

## 阶段 4：实现

一旦根因被确认（不是猜测）：

### 4.1 最小修复
- 应用**尽可能小的变更**来修复根因
- 不要重构周围的代码
- 不要添加功能
- 不要"改善"你在调查过程中注意到的东西

### 4.2 回归测试
编写一个测试：
1. 在修复前**失败**（验证测试确实能捕获 bug）
2. 在修复后**通过**
3. 在测试名称中描述 bug 场景

```bash
# 验证：修复前测试失败
git stash && {{ ci_command }} 2>&1 | grep -i "fail\|error"; git stash pop
# 验证：修复后测试通过
{{ ci_command }}
```

### 4.3 完整测试套件
运行完整的测试套件以验证没有回归：
```bash
{{ ci_command }}
```

---

## 阶段 5：验证与报告

### 5.1 结构化调试报告

输出结构化报告：

```
INVESTIGATION REPORT
═══════════════════════════════════════════════════════════

SYMPTOMS:
  [观察到了什么]

ROOT CAUSE:
  [实际原因是什么，引用具体的 file:line]

FIX:
  [做了什么变更以及为什么]

EVIDENCE:
  [运行的命令、观察到的输出、测试结果]

REGRESSION TEST:
  [测试名称及其覆盖内容]

RELATED RISKS:
  [其他可能存在相同模式的地方]

STATUS: [DONE / DONE_WITH_CONCERNS / BLOCKED]
═══════════════════════════════════════════════════════════
```

### 5.2 状态码

- **DONE**：找到根因，应用修复，测试通过，已添加回归测试
- **DONE_WITH_CONCERNS**：已修复但其他地方可能存在类似问题（列出它们）
- **BLOCKED**：3 次规则后无法确认根因。需要人工输入。

---

## 重要规则

- **先调查再修复。** 始终如此。
- **范围锁定。** 只修改受影响的代码。
- **3 次规则。** 3 个失败的假设 → 停止并升级。
- **需要回归测试。** 每个修复都需要一个本应捕获该 bug 的测试。
- **不做顺便修复。** 记录下来留到以后，现在不做。
- **最小 diff。** 修复应尽可能小。
