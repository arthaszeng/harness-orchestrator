{# _plan-content.md.j2 — aggregates the full plan lifecycle.
   Included by: _vision-content.md.j2, skill-plan.md.j2
   Contains: plan-core → plan-review → review-gate → ship invocation #}

{% include 'sections/_plan-core.md.j2' %}

---

{% include 'sections/_plan-review.md.j2' %}

---

{% include 'sections/_review-gate.md.j2' %}

---

## 阶段交接 — Plan → Build

在调用 ship 之前，将**阶段摘要**写入 `workflow-state.json` 的 `handoff_summary` 字段，
让 build 阶段获得紧凑上下文而不必重新阅读完整计划。

摘要内容（≤ 2000 字符）：2-4 句概述要构建什么、关键方案、约束、已做的决策和风险。

如果 `workflow-state.json` 不存在，跳过 — handoff 为可选增强。

---

## 执行

Review gate 已通过。调用 `/harness-ship` 执行完整的
build → eval → ship → PR 管线。

**衔接保障**：plan 阶段已将 `workflow-state.json` 的 `phase` 更新为 `contracted`
并写入了 `handoff_summary`。Ship 检测到这些信号后会进入**快速路径**——
跳过已在 plan 中完成的预检（分支检查、config 检查、任务目录扫描），
直接从 rebase 和测试开始。无需手动传递上下文。

Ship 会派遣同样的 5 个角色（architect、product-owner、engineer、
qa、project-manager）以 `code-eval` 模式进行评审阶段。

硬性完成规则：
- 不得在 Build 结束后停下，也不得在中间里程碑询问“是否继续”。
- 必须自主连续执行到产出 PR URL，除非触发本工作流顶部定义的显式停止条件。
- 若测试/eval/gate 失败，或命令非零退出且无法自动修复，必须停止并报告阻塞，禁止强行推进。
