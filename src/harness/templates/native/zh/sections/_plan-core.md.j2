## 计划生成

### 核心原则

{{ planner_principles }}

### 6 个决策原则

做规划决策时，按顺序应用以下原则：

1. **选择完整性** — 有疑问时，覆盖更多边界情况而非更少。
2. **彻底清理** — 如果你在修复影响范围内的某个问题，把所有相关问题一并修复。
3. **务实** — 当两种方案解决同一问题时，选择更简洁的。
4. **DRY** — 如果计划重复已有功能，停下来。复用已有的。
5. **显式优于巧妙** — 10 行一目了然的代码胜过 200 行抽象框架。
6. **偏向行动** — 合并优于再来一轮审查优于停滞不前。

### 决策分类

规划期间的每个决策归入以下三类之一：

- **机械性的** — 只有一个正确答案。直接应用，无需讨论。
- **品味** — 合理的工程师可能意见不同。标记为 `[TASTE DECISION]`。
- **用户挑战** — 两位审查者都认为用户的方向需要重新考虑。
  绝对不要自行决定。必须带着证据向用户呈现。

### 上下文收集

1. 仔细阅读用户的需求
2. 读取 `.harness-flow/config.toml` 和 `.harness-flow/vision.md`（如果存在）
3. 扫描项目结构：关键目录、语言、框架、已有模式
4. 运行 `git log --oneline -10` 了解最近的活动上下文
5. 检查已有任务目录：`ls .harness-flow/tasks/` 确定下一个任务编号

### 生成 Spec + 合约

将输出写入 `.harness-flow/tasks/task-NNN/plan.md`（如需要则创建目录）。
如果该工作流使用任务级机器可读状态，请初始化或更新
`.harness-flow/tasks/task-NNN/workflow-state.json`，把它作为 phase、gate、blocker 与
artifact refs 的 canonical 记录。会话摘要、registry 或 event 日志不得成为平行 gate 来源。

```markdown
# Spec

## Analysis
（需求的技术分析 — 解决什么问题、为什么重要）

## Approach
（实现方案、关键决策、考虑过的替代方案及拒绝原因）

## Impact
（受影响的文件、影响范围、依赖关系）

## Risks
（潜在风险和缓解措施 — 可能出什么问题）

---

# Contract

## Deliverables
- [ ] 交付物 1：描述 + 验收标准
- [ ] 交付物 2：描述 + 验收标准
...

## Acceptance Criteria
- 所有测试通过
- 没有引入回归
- （来自需求的具体标准）

## Out of Scope
- （明确列出不做的事项）

## Decision Audit Trail
| # | Phase | Decision | Classification | Principle | Rationale | Rejected Alternative |
|---|-------|----------|---------------|-----------|-----------|---------------------|
| 1 | Approach | ... | Mechanical/Taste | #N | ... | ... |
```

**质量标准**：每个交付物必须有验收标准。模糊的交付物如"改善代码"是不可接受的 — 必须具体且可测试。
