## 计划生成

### 核心原则

{{ planner_principles }}

### 6 个决策原则

做规划决策时，按顺序应用以下原则：

1. **选择完整性** — 有疑问时，覆盖更多边界情况而非更少。
2. **彻底清理** — 如果你在修复影响范围内的某个问题，把所有相关问题一并修复。
3. **务实** — 当两种方案解决同一问题时，选择更简洁的。
4. **DRY** — 如果计划重复已有功能，停下来。复用已有的。
5. **显式优于巧妙** — 10 行一目了然的代码胜过 200 行抽象框架。
6. **偏向行动** — 合并优于再来一轮审查优于停滞不前。

### 决策分类

规划期间的每个决策归入以下三类之一：

- **机械性的** — 只有一个正确答案。直接应用，无需讨论。
- **品味** — 合理的工程师可能意见不同。标记为 `[TASTE DECISION]`。
- **用户挑战** — 两位审查者都认为用户的方向需要重新考虑。
  绝对不要自行决定。必须带着证据向用户呈现。

### 上下文收集（可并行化）

尽可能并行收集上下文 — 以下步骤之间无数据依赖：

1. 仔细阅读用户的需求
2. 读取 `.harness-flow/config.toml` 和 `.harness-flow/vision.md`（如果存在）
3. 扫描项目结构：关键目录、语言、框架、已有模式
4. 运行 `git log --oneline -10` 了解最近的活动上下文
5. 确定下一个任务编号：运行 `harness task next-id`（或 `harness task next-id --json` 获取 `{"next_id": "task-NNN", "max_existing": N}`）

步骤 2–5 可以在同一条消息中作为并行工具调用发出。

### 轻量计划模式

编写计划后，使用以下命令验证结构：

```bash
harness plan-lint --task TASK_ID --json
```

CLI 返回 `{valid, errors[], plan_mode}`，其中 `plan_mode` 为 `small`、`medium` 或 `large`，
基于交付物数量和预计文件规模。使用 `plan_mode` 来校准下游评审强度（如小型计划使用 FAST）。

### 生成 Spec + 合约

将输出写入 `.harness-flow/tasks/task-NNN/plan.md`（如需要则创建目录）。
如果该工作流使用任务级机器可读状态，请初始化或更新
`.harness-flow/tasks/task-NNN/workflow-state.json`，把它作为 phase、gate、blocker 与
artifact refs 的 canonical 记录。会话摘要、registry 或 event 日志不得成为平行 gate 来源。

```markdown
# Spec

## System Design Thinking

在写 Analysis 之前，先搜索历史失败模式，然后用搜索结果来支撑设计思考。

**第 1 步——搜索与本任务领域相关的先验失败模式：**

```bash
harness search-failures --query "<描述本任务领域的关键词>" --limit 10
```
{% if memverse_enabled == "true" %}

同时查询 Memverse 获取更广泛的跨项目模式（项目域 + 五个 `*-learning` 域）：

```
CallMcpTool(
  server="user-Memverse",
  toolName="search_memory",
  arguments={
    "query": "<描述本任务领域的关键词>",
    "domain": "{{ memverse_domain }},{{ memverse_domain }}-architect-learning,{{ memverse_domain }}-engineer-learning,{{ memverse_domain }}-qa-learning,{{ memverse_domain }}-product-owner-learning,{{ memverse_domain }}-project-manager-learning",
    "metadata_filter": "{\"type\": \"failure-pattern\"}",
    "limit": 10
  }
)
```
{% endif %}

**第 2 步——回答以下三个问题，融入上面搜索到的失败模式：**

### 核心挑战
这类系统的第一性原理挑战是什么？不是"需要实现什么功能"，而是"为什么这类系统难做对"。
如果搜索到了失败模式，引用具体的失败来揭示这些挑战。

### 架构约束
哪些设计约束是非谈判性的？每条标注理由。
如果失败模式提到了具体的违规（例如"直接调用 time.sleep" → 可测试性），将其提升为明确的约束。
常见约束参考（仅保留与当前任务相关的）：
- 可测试性（sleep/时间函数可注入，避免测试中的真实等待）
- 幂等性（相同输入多次执行产生相同结果）
- 并发安全（多线程/多进程安全，无竞态条件）
- 可注入性（外部依赖通过接口注入，不硬编码）
- 优雅关停（进行中的工作能安全完成或回滚）

### 设计陷阱
这类系统常见的设计陷阱是什么？列出 2-3 个最可能踩到的坑。
如果失败模式描述了过去的错误，将其列为具体的陷阱，而非通用描述。

## Analysis
（需求的技术分析 — 解决什么问题、为什么重要。引用上面的 Design Thinking 结论。）

## Approach
（实现方案、关键决策、考虑过的替代方案及拒绝原因。解释如何满足上面的架构约束。）

## Impact
（受影响的文件、影响范围、依赖关系）

## Risks
（潜在风险和缓解措施 — 可能出什么问题）

---

# Contract

## Design Principles
（从 System Design Thinking 提炼出的 3-5 条具体设计原则，Builder 必须遵守。
每条原则必须可验证——能通过代码审查或测试确认。）

- [ ] DP1：（例：sleep/时间函数通过参数注入，不直接调用 time.sleep）
- [ ] DP2：（例：每次投递使用 delivery_id 幂等锁，重复请求直接返回）
- ...

## Deliverables
- [ ] 交付物 1：描述 + 验收标准
- [ ] 交付物 2：描述 + 验收标准
...

## Acceptance Criteria
- 所有测试通过
- 没有引入回归
- Design Principles 全部被实现满足
- （来自需求的具体标准）

## Out of Scope
- （明确列出不做的事项）

## Decision Audit Trail
| # | Phase | Decision | Classification | Principle | Rationale | Rejected Alternative |
|---|-------|----------|---------------|-----------|-----------|---------------------|
| 1 | Approach | ... | Mechanical/Taste | #N | ... | ... |
```

**质量标准**：每个交付物必须有验收标准。模糊的交付物如"改善代码"是不可接受的 — 必须具体且可测试。
