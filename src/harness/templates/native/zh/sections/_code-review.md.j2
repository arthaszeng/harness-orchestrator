## 代码评审 — 5 角色并行派遣

### 派遣

通过 Task 工具**并行启动所有 5 个审查者**，每个使用 `mode: code-eval`。
发送一条包含所有 5 个 Task 工具调用的消息：

| # | 子代理 | 推荐模型 | 必需 context | 可选 context |
|---|--------|---------|-------------|-------------|
| 1 | `harness-architect` | 不指定（继承父代理模型） | diff（`{{ trunk_branch }}..HEAD`）、架构文档 | 依赖清单 |
| 2 | `harness-product-owner` | {% if role_models_product_owner %}不指定（继承父代理模型）{% else %}`model: "fast"`{% endif %} | plan、diff `--stat` 摘要、`.harness-flow/vision.md` | roadmap/backlog/feedback；当 stat 与 plan 明显不匹配时升格读取关键 diff 片段 |
| 3 | `harness-engineer` | 不指定（继承父代理模型） | diff（`{{ trunk_branch }}..HEAD`）、源代码目录列表 | — |
| 4 | `harness-qa` | {% if role_models_qa %}不指定（继承父代理模型）{% else %}`model: "fast"`{% endif %} | diff（`{{ trunk_branch }}..HEAD`）、测试目录列表、`ci_command: {{ ci_command }}` | — |
| 5 | `harness-project-manager` | {% if role_models_project_manager %}不指定（继承父代理模型）{% else %}`model: "fast"`{% endif %} | plan、diff `--stat` + commit log、交付物清单 | feedback/continue-pause 上下文 |

**禁止附加（按角色预算）：** 组合 Task 提示词时，父代理**不得**：

- 把**同一份完整** `git diff {{ trunk_branch }}..HEAD` 贴进**每一个**子代理——仅 Architect、Engineer、QA 按表格需要完整 diff。
- 默认向 **Product Owner** 或 **Project Manager** 提供**完整**统一 diff。二者仅接收 `plan`、`git diff ... --stat`、commit log、交付物清单；仅当统计与 plan 明显矛盾时按「可选 context」追加**少量针对性 diff 片段**。
- 向任一 Task 附加无关长会话记录、流水日志或**其他角色的完整评审原文**——只放该行 Required/Optional 所列内容。

**模型控制**：Task 工具 `model` 参数仅接受 `"fast"` 或省略（继承父代理模型）。
当 `[native.role_models]` 为某角色配置了模型时，生成的 agent 定义会包含 `model:` 字段
作为 IDE 级别的信号；此时省略 Task `model` 参数以让 agent 定义生效。
未配置时，传递 `model: "fast"` 以实现低成本派遣。

**Context 最小化**：构建每个子代理的 Task prompt 时，**仅包含 agent 模板中
"评审代码时（mode: code-eval）" 段落**作为角色指令。不要嵌入 "评审计划时" 段落——那是 plan-review 模式专用的。

每个 prompt 必须包含：
- `response_language: {{ artifact_lang }}`
- `diff_range: {{ trunk_branch }}..HEAD`
- `plan_path: .harness-flow/tasks/task-NNN/plan.md`（如可用）
{% if memverse_enabled == "true" %}
- `memverse_domain: {{ memverse_domain }}`
{% endif %}
- 上表中该角色的"必需 context"
- 当工作流包含长期方向治理时：
  - 对 PO 额外提供 `roadmap_backlog_path` 与 `feedback_ledger_path`（或等价摘要）
  - 对 PM 额外提供 `feedback_ledger_path` 与 `continue_pause_summary`（或等价摘要）
  - 如果这些上下文不存在，则保持共享 section 的默认单轮语义，不要强行引入 loop 结论

**CI 所有权：** 只有 `harness-qa` 运行 `{{ ci_command }}`。其他角色
对 diff 执行只读分析。

**Task 提示词组合（固定协议 vs 本次事实）**

- **固定协议：** 子代理行为**只能**来自 agent 模板中 **「评审代码时（mode: code-eval）」** 对应段落（或英文等价标题）。禁止在 Task 正文中改写角色职责。
- **本次事实：** 仅附加表格允许的内容——diff 范围、stat 摘要、路径、有边界的片段、`ci_command`、目录列表。若角色只需清单，优先指针而非重复粘贴整份 plan。
- **禁止（广播）：** 不要把**相同的大块**上下文（整份 plan + 整份 diff + 整份日志）复制进**全部五个** Task；按每行的必需/可选列裁剪。

**降级阶梯**（如果 Task 工具调用失败）：
- **5/5 响应：** 按下述方式完整合成
- **3-4/5 响应：** 使用可用的评审结果继续，注明缺失的视角
- **1-2/5 响应：** 记录警告，降级为单代理评审
- **0/5 响应：** 回退到单个 generalPurpose 子代理评审

### 合成

所有响应到达后：

1. **归一化并聚类发现** — 归一化表述（去掉套话）；将**同一根因**的发现归为一簇；记录每簇由哪些角色贡献（用于 `[HIGH CONFIDENCE]`，并与下方重复签名惩罚对齐）。
2. **合并发现** — 由簇生成统一列表（每簇一条规范表述，保留角色来源/标签）。
3. **过滤超范围发现** — 使用角色→职责映射：
   | 角色 | 负责 | 不得报告 |
   |------|------|---------|
   | Architect | 设计、模块、依赖、安全设计 | 代码质量、测试、交付、用户价值 |
   | Product Owner | 用户价值、需求、AC、UX 边界情况 | 代码质量、架构、测试、交付 |
   | Engineer | 代码质量、DRY、模式、性能、错误处理 | 架构决策、用户价值、交付、测试策略 |
   | QA | 测试、覆盖率、CI、回归、边界 | 代码质量、架构、用户价值、交付 |
   | Project Manager | 范围、交付、分解、提交结构 | 代码质量、架构、测试、用户价值 |

   对每个发现，检查报告角色是否负责该类别：
   - **范围内**：保持原样
   - **范围外 + CRITICAL**：重新标记为 `[CROSS-ROLE]` 并保留
   - **范围外 + WARN/INFO**：标记 `[OUT-OF-SCOPE]` 并从评分中排除
4. **交叉验证：** 如果 2+ 个角色标记同一问题（或同一簇） → 标记 `[HIGH CONFIDENCE]`
5. **方向/治理合成：** 保持角色建议字段并列，不要合并为单一方向字段：
   - Product Owner → `Value recommendation`
   - Project Manager → `Delivery recommendation`
6. **计算维度评分**（将角色映射到评审维度）：
   ```
   Design:        [architect score]/10
   Completeness:  [product-owner score]/10
   Quality:       [engineer score]/10
   Regression:    [qa score]/10
   Scope:         [project-manager score]/10
   ─────────────────────────────────
   Weighted avg:  [avg]/10
   ```
7. **确定裁定：**
   - 全部 PASS 且 avg ≥ {{ pass_threshold }} → `PASS`
   - 任何 CRITICAL 发现 → `ITERATE`
   - avg < {{ pass_threshold }} → `ITERATE`

### 分数校准规则（提升信号区分度）

在最终落分前应用以下规则：
- 跨轮次重复出现的问题签名会降低分数置信度：
  - 每个重复签名：`-0.30`
  - 重复惩罚总和上限 `-1.50`
- 若重复项被标记为 `[HIGH CONFIDENCE]`，额外 `-0.50`。
- 不改变 PASS/ITERATE 的门禁语义：
  - 出现 CRITICAL 仍然强制 `ITERATE`
  - 阈值判断仍使用 `{{ pass_threshold }}`。

### Fix-First 分类

应用 `.cursor/rules/harness-fix-first.mdc` 中的 Fix-First 启发式：

- **AUTO-FIX：** 高确定性 + 小影响范围 + 可逆 → 立即修复
- **ASK：** 安全、行为变更、架构变更、数据模型变更、
  或低置信度 → 批量呈现给用户

自动修复后，重新运行 `{{ ci_command }}` 以验证无回归。
记录：`[AUTO-FIXED] file:line — 问题 → 所做的修复`

### 最终裁定

**使用 CLI 保存评审结果**（必须执行 — 不可跳过）：

```bash
harness save-eval --kind code --task task-NNN --verdict PASS --score 8.5 --body "$(cat <<'EVAL_EOF'
# Code Evaluation — Round N

## Dimension Scores
| Dimension | Role | Score |
|-----------|------|-------|
| Design | architect | N/10 |
| Completeness | product-owner | N/10 |
| Quality | engineer | N/10 |
| Regression | qa | N/10 |
| Scope | project-manager | N/10 |
| **Weighted Average** | | **N/10** |

## Findings
[merged findings list]

## Auto-Fixed
[list of auto-fixed items]

## ASK Items
[items requiring user decision]

## Verdict: PASS | ITERATE
EVAL_EOF
)"
```

将 `task-NNN`、分数和 `PASS`/`ITERATE` 替换为实际值。
命令会以自动递增的轮次编号创建 `code-eval-rN.md`。
