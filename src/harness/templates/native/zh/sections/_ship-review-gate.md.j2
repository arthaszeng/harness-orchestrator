## Ship Review Gate — 自适应代码评审强度

在派遣代码审查者之前，运行**确定性 escalation CLI** 计算评审强度。
替代手动信号算术，毫秒级完成。

### Ship Escalation Score

运行预计算命令（设计为在 CI 后台运行期间执行）：

```bash
harness ship-prepare --task TASK_ID --json
```

返回 `{diff_stat, escalation: {score, level, signals}, review_dispatch: {level, roles}}`。

或仅计算 escalation score：

```bash
harness escalation-score compute --phase ship --json
```

CLI 读取 `git diff/log`，计算信号（diff 规模、文件数、风险目录、API 表面、commit 数）+ trust 调整。

### Barrier 注册

派遣评审前，为将并行运行的 sidecar 任务注册 barrier：

```bash
harness barrier register --task TASK_ID --id code-review --phase ship --required
harness barrier register --task TASK_ID --id memverse-sync --phase ship
harness barrier register --task TASK_ID --id scope-drift --phase ship
```

这些 barrier 会在 `harness gate` 允许 ship 前被检查。

### 评审强度级别

**评分 >= {{ gate_full_review_min }} → FULL（完整评审）**
并行派遣全部 5 个角色审查者（Architect、Product Owner、Engineer、QA、Project Manager）。
这是标准行为 — 继续到下方的代码评审派遣部分。
记录：`SHIP_GATE: FULL (score N) — 派遣完整多角色评审`

**评分 {{ gate_summary_confirm_min }}–{{ (gate_full_review_min | int) - 1 }} → LITE（轻量评审）**
仅派遣 **2 个角色审查者**：**Engineer** 和 **QA**。
这两个角色是代码质量和回归安全最直接的守护者。
本轮跳过 Architect、Product Owner 和 Project Manager。
使用下方相同的代码评审派遣格式，但仅针对选定的 2 个角色。
记录：`SHIP_GATE: LITE (score N) — 派遣 Engineer + QA 评审`

**评分 < {{ gate_summary_confirm_min }} → FAST（快速通过）**
完全跳过多角色代码评审。CI 测试和 `harness gate`（机器级检查）仍然执行 —
这仅绕过 LLM 角色评审。

**注意：** 当启用渐进式信任阈值（`apply_trust_threshold = true`）时，
`harness gate` 会将 eval 分数与信任调整后的有效阈值比较
（基准 {{ pass_threshold }} ± 信任调整量，clamp 到 [5.0, 10.0]）。
使用 `harness trust` 查看当前信任等级和阈值调整量。

写入一个最小的 eval 产物以便 `harness gate` 能找到：
```bash
harness save-eval --kind code --task TASK_ID --verdict PASS --score {{ pass_threshold }} --body "$(cat <<'FAST_EVAL_EOF'
# Code Evaluation — Fast Pass

Ship escalation score: N（低于 {{ gate_summary_confirm_min }}）
评审强度：FAST — 根据自适应门禁策略跳过多角色评审。
CI 测试：已通过。
机器门禁：将单独检查。

## Verdict: PASS
FAST_EVAL_EOF
)"
```
记录：`SHIP_GATE: FAST (score N) — 跳过多角色评审，仅 CI + gate`

### 与现有步骤的交互

- **FULL**：正常进入代码评审部分（全部 5 个角色）。
- **LITE**：使用代码评审部分，但仅派遣 Engineer + QA。合成步骤应注明仅派遣了 2 个角色并相应调整预期。
- **FAST**：完全跳过代码评审部分。直接跳到步骤 3.9（捕获学习成果）或步骤 4（范围漂移检测）。
- **`harness gate`（步骤 5.9）** 无论强度级别都无条件运行。
- **Memverse 学习捕获（步骤 3.9）**：LITE 模式下仅有 Engineer 和 QA 的学习成果可用。FAST 模式下不捕获角色特定的学习成果。
