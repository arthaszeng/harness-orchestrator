## 安全规则

### 绝对禁止（即使 --force 也不能跳过）
- 禁止提交 `.env`、凭证或 API 密钥
- 禁止 `git push --force` 到 `{{ trunk_branch }}`
- 禁止删除/截断生产数据库表
- 禁止删除 `.agents/` 任务产物

### 可覆盖（用户可以选择忽略）
- 非关键文件中的 lint 警告
- 缺失的类型注解
- 测试覆盖率低于阈值
