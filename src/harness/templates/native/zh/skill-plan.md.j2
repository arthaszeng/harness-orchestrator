---
name: harness-plan
description: "从需求到 PR 的端到端流程（多数任务的默认主入口）：通过多角色评审完善计划，然后自动执行 build/eval/ship/retro。当用户说 plan、做这个、实现、构建这个功能、harness plan 或提供具体需求时使用。(harness)"
---

{% include 'sections/_language-directive.md.j2' %}
{% include 'sections/_git-strategy-matrix.md.j2' %}
{% include 'sections/_continuity-mandate.md.j2' %}

# Harness Plan — 从需求到 PR

你正在运行 `/harness-plan` 工作流。用户有一个**具体的需求**，
需要规划和执行。

**人机交互阶段：** 计划完善 + Review Gate
**自主阶段：** Build → Eval → Ship → Auto-Retro

**仅在以下情况停止：**
- 需求模糊（请求澄清**一次**，然后以最佳理解继续）
- 计划评审识别出的品味决策或用户挑战
- Review gate 升级（当 plan_review_gate 要求时）
- 评审中 ASK 类发现

**绝不因以下原因停止：**
- 计划格式审批
- 写入磁盘前的确认
- 实现决策（在 build 阶段做出）
- AUTO-FIX 发现（静默修复）
- 仅交付计划产物就结束回合 —— Review Gate 通过后，必须在**同一对话**内开始 **`/harness-build`**（不要等用户再说「继续 build」）。
- Build 完成后的阶段检查或“是否继续”确认 —— 一旦进入 Build，必须无缝继续 `/harness-ship`，无需等待用户确认。

---

{% include 'sections/_workflow-progress-envelope.md.j2' %}

{% with preflight_mode="entry" %}
{% include 'sections/_workspace-preflight.md.j2' %}
{% endwith %}

---

## 阶段 0：需求理解

### 默认主入口（请先读）

本工作流是大多数任务的**默认主入口**。需求略模糊时，**一轮**澄清（必要时辅以极小范围上下文阅读），然后继续规划。仅在用户明确要求长期发散、持续 roadmap/backlog 治理，或以愿景驱动多轮推进时，再建议 `/harness-vision`；不要作为会话中段的强制打断。

阅读用户的需求。如果模糊，请求澄清**一次** —
然后以最佳理解继续。

---

{# Recursive composition: plan = Phase 0 + plan-content (plan-core, plan-review, gate, ship) #}
{% with interaction_depth="low" %}
{% include 'sections/_plan-content.md.j2' %}
{% endwith %}

---

{% include 'sections/_error-recovery.md.j2' %}

{% include 'sections/_bypass-immunity.md.j2' %}
